apiVersion: v1
kind: Service
metadata:
  name: {{ include "mem-weaver.fullname" . }}
  labels:
    {{- include "mem-weaver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "mem-weaver.selectorLabels" . | nindent 4 }}
